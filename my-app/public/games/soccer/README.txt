Place your Blender export here as stadium.glb (used by default).

Gameplay uses FIFA 68x105m bounds. White lines + goal posts are drawn on top so
they always match physics. If stadium.glb fails to load, a built-in pitch is used.
